# Contributing

欢迎通过 Issue 或 Pull Request 改进 Aigoni。

提交前请确认：

1. Go 代码符合 Google Go Style。
2. 行为变更同步更新 README 或 docs 中对应说明。
3. 不提交 `.env`、私人内容、生成 Wiki、上传资源、`.trellis/`、`.claude/` 或本地二进制文件。
4. 提交前运行：

```sh
go test ./...
go build ./...
```

文档请保持简洁清晰。
